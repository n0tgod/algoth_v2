#!/usr/bin/env python3
"""Бумажные DCA-книги: одни правила, три депозита ($1k / $10k / $100k).

Что это и чем НЕ является. Книга ведёт запись ВПЕРЁД: суточный прогон
дописывает в журнал решения, чьи исходы уже закрылись, и помечает каждое
моментом записи. Решение, попавшее в журнал позже `rules.AHEAD_H` часов
после самого себя, есть ПЕРЕСЧЁТ по прошлому, а не наблюдение, и в одну
сумму с наблюдением не идёт никогда (`rules.split_rows`). Первый прогон
восстанавливает всю накопленную историю — она вся помечена пересчётом.

Живого исполнения здесь нет: сделки считаются реплеем по барам записи, а
не сканером на живой цене. Что для настоящего живого контура пришлось бы
достроить, названо в отчёте, а не подразумевается.

Книг девять: три режима × три депозита. По депозиту книги отличаются
РОВНО им — но билет НЕ один и тот же: он есть максимум из пола режима и
доли депозита, которой хватает на все ЕГО места (`rules.ticket`).
Формула одна на все режимы, различаются их собственные пол (из худшего
плеча режима) и пик (измеренный по журналу): у режимов без гейта пол $25
и пик 457, у режима с гейтом $6.25 и 237. По режиму отличается то, из чего выводится
плечо (`rules.RULERS`): «безопасная» считает запас от собственной σ
имени, «оптимальная» — от глубины лестницы, «агрессивная» берёт ту же
глубину и добавляет ГЕЙТ ВХОДА по плечу (`min_lev`). Все три ведутся
параллельно: какая лучше, решает форвард.

Биржевое правило соблюдается с первого дня: у имени позиция ОДНА, второй
выбор по той же монете пропускается (`rules.ONE_PER_NAME`).

Прогон: `run research/dca_paper/run_paper.py`. Смоук: `--limit 400`.
Публикует отчёт сам; `--no-publish` выключает.
"""

import argparse
import json
import os
import subprocess
import sys
import time

import numpy as np

HERE = os.path.dirname(os.path.abspath(__file__))
ROOT = os.path.dirname(os.path.dirname(HERE))      # корень репозитория
sys.path.insert(0, HERE)
sys.path.insert(0, os.path.join(ROOT, "research", "dca_ladder"))
sys.path.insert(0, os.path.join(ROOT, "research", "s8_loop"))
import rules as R                                             # noqa: E402
import run_d6 as D6                                           # noqa: E402
import tail as TL                                             # noqa: E402

# Линейки плеча объявлены В ПРАВИЛАХ, а не здесь: их читает и страница
# наблюдения, и вторая запись однажды разошлась бы с той, по которой
# книга торгует.
# Пара реплея несёт СТОРОНУ третьим полем: длинная и короткая книги
# одной линейки считают разные позиции по разным ногам, и общий ключ
# кэша склеил бы их в одну.
RULERS = {k: (v["rule"], v["param"], R.side_of(k)) for k, v in R.RULERS.items()}


def cache_path():
    """Путь кэша разрешается В МОМЕНТ ВЫЗОВА, а не на импорте.

    Замороженный на импорте путь уже подводил проект (реестр складов
    Z2/Z3): прогон с подменённым каталогом молча писал бы мимо него.
    """
    return os.path.join(R.OUT, "recs.jsonl")     # кэш реплея, в git не идёт


def cache_sig():
    """Подпись настроек, ОТ КОТОРЫХ ЗАВИСИТ РЕПЛЕЙ.

    Кэш законен ровно потому, что прошлое не меняется: закрытая позиция
    при тех же правилах даст тот же исход. Смени правило геометрии — и
    кэш описывает другую книгу, поэтому подпись сверяется, а расхождение
    гонит полный пересчёт вслух, а не молча.
    """
    import run_d2 as D2
    return {"edge": D2.MIN_EDGE_BP, "rr": D2.MIN_RR, "hold_h": D2.HOLD_H,
            "back_h": D2.BACK_H, "rungs": D2.N_RUNGS,
            "weights": list(D2.WEIGHTS), "gap": D2.MIN_ADD_GAP,
            "floor": D2.FLOOR_FRAC, "survive": R.SURVIVE_MULT,
            "sigma_mult": R.SIGMA_MULT, "rulers": sorted(RULERS.values()),
            # Тейк — правило ИСХОДА, а не ускорение: кэш, посчитанный на
            # прежней цели, описывает другую книгу и обязан быть отвергнут
            # вслух (правка 2026-09-05, замер D8).
            "take": [R.TAKE_ANCHOR, R.TAKE_MULT],
            # Хвост ленты, продолженный серединой стакана (`tail.py`), —
            # это ПРАВИЛО исхода, а не ускорение: кэш, посчитанный без
            # него, описывает другую книгу и обязан быть отвергнут вслух.
            "tail": 1,
            # Предел плеча ТИРА площадки: забор без него выдавал
            # плечо, которого биржа не даёт, — это не консервативная
            # оценка, а сделка, которой не было. Кэш, посчитанный без
            # предела, описывает другую книгу.
            "lev_cap": 1}


def read_cache(path=None):
    """Кэш реплея: (пара, символ, момент) → запись позиции.

    Возвращает (кэш, причина непригодности). Непригодный кэш не чинится
    молча — прогон говорит словами и считает всё заново.
    """
    path = path or cache_path()
    if not os.path.exists(path):
        return {}, "кэша нет"
    want = json.dumps(cache_sig(), sort_keys=True, ensure_ascii=False)
    out, sig = {}, None
    try:
        with open(path, encoding="utf-8") as f:
            for i, ln in enumerate(f):
                ln = ln.strip()
                if not ln:
                    continue
                r = json.loads(ln)
                if i == 0 and r.get("sig") is not None:
                    sig = json.dumps(r["sig"], sort_keys=True,
                                     ensure_ascii=False)
                    if sig != want:
                        return {}, "правила реплея изменились"
                    continue
                out[(tuple(r["pair"]), r["sym"], round(float(r["at"]), 3))] = r
    except (OSError, ValueError) as e:
        return {}, f"кэш не читается: {e}"
    if sig is None:
        return {}, "кэш без подписи правил"
    return out, None


def write_cache(cache, path=None):
    """Кэш пишется ЦЕЛИКОМ и атомарно: дозапись оставила бы в файле
    записи двух подписей разом, а различить их потом нечем."""
    path = path or cache_path()
    tmp = path + ".tmp"
    with open(tmp, "w", encoding="utf-8") as f:
        f.write(json.dumps({"sig": cache_sig()}, ensure_ascii=False) + "\n")
        for (pair, sym, at), r in cache.items():
            r = dict(r, pair=list(pair), sym=sym, at=at)
            f.write(json.dumps(r, ensure_ascii=False) + "\n")
    os.replace(tmp, path)


def needs_replay(cache, legs, pairs):
    """Какие решения обязан пересчитать этот прогон.

    Правило одно и оно про ВРЕМЯ, а не про экономию: закрытая позиция
    окончательна — прошлые бары не меняются, значит её исход при тех же
    правилах будет тот же. Всё остальное считается заново КАЖДЫЙ прогон:
    у открытой отметка меняется с ценой, и вчерашняя была бы ложью, а
    оборванная («cut») ждёт продолжения записи.

    Решение нужно целиком, если хоть одной линейке его недостаёт: пары
    считаются одним проходом по барам, и делить его по линейкам значило
    бы читать те же бары дважды.
    """
    need = []
    for g in legs:
        sym, at = str(g["sym"]), round(float(g["at"]), 3)
        side = g.get("side") or "long"
        for pr in pairs:
            # Пара ЧУЖОЙ стороны этой ноге не положена вовсе, и требовать
            # её в кэше значило бы объявлять каждую ногу непосчитанной
            # навсегда — полный пересчёт каждый прогон, молча.
            if (pr[2] if len(pr) > 2 else "long") != side:
                continue
            r = cache.get((tuple(pr), sym, at))
            if r is None or r.get("state") != "closed":
                need.append(g)
                break
    return need


def _key(r):
    """Ключ решения: имя плюс секунда входа. Ими и дедуплицируется."""
    return f"{int(r['at'])}:{r['sym']}"


def _cell(ruler, dep):
    """Ключ книги: линейка и депозит. Одно решение живёт в обеих книгах,
    и склеив их одним ключом, мы потеряли бы вторую целиком."""
    return f"{ruler}:{int(dep)}"


def build_rows(by_ruler, now=None, log=print):
    """Решения, взятые каждой книгой, с деньгами в долларах.

    Одна позиция на имя применяется ДО раздачи кассы: правило биржи не
    зависит от депозита, и применив его после, мы дали бы разным книгам
    разные составы по чужой причине. По линейкам состав РАЗЛИЧАЕТСЯ
    законно: ограда отказывает по-разному, и это свойство линейки, а не
    артефакт — числа отказов печатаются по каждой отдельно.
    """
    now = float(now if now is not None else time.time())
    out, cells, one, live = [], {}, {}, {}
    for rk in R.RULER_ORDER:
        recs = by_ruler.get(rk) or []
        # Гейт плеча — ПЕРВЫМ, до правила одной на имя. Порядок решает
        # состав: у режима с гейтом низкоплечевой ранний вход просто не
        # случается, значит имя свободно, и позже по нему может открыться
        # рычажный. Применив гейт после, мы отдали бы слот входу, который
        # этот режим не берёт вовсе.
        ml = R.min_lev_of(rk)
        gated = ([r for r in recs if float(r["lev"]) >= ml]
                 if ml is not None else list(recs))
        keep, skipped = (D6.one_per_name(gated) if R.ONE_PER_NAME
                         else (list(gated), 0))
        # Пик СВОЕЙ книги: из него и считается билет режима. Измеренный
        # печатается рядом с объявленным — объявленный ниже измеренного
        # означает, что билет велик и часть решений уходит по кассе.
        pk = D6.peak_open(keep) if keep else {}
        levs = sorted(float(r["lev"]) for r in keep)
        seen = pk.get("names_max")
        one[rk] = {"positions": len(recs), "kept": len(keep),
                   "skipped_repeats": skipped,
                   "min_lev": ml, "gate_dropped": len(recs) - len(gated),
                   "peak_names": seen,
                   "peak_declared": R.peak_of(rk),
                   "floor": round(R.floor_of(rk), 4),
                   "ticket": {str(int(d)): R.ticket(d, rk)
                              for d in R.DEPOSITS},
                   # объявленный пик обязан быть НЕ НИЖЕ измеренного:
                   # ниже — билет велик, и книга теряет решения по кассе
                   "peak_over": bool(seen is not None
                                     and seen > R.peak_of(rk)),
                   "lev_median": (round(levs[len(levs) // 2], 2)
                                  if levs else None)}
        log(f"линейка {R.ruler_title(rk)} ({rk}): позиций {len(recs)}"
            + (f", гейт плеча ≥{ml:g}× отсеял {len(recs) - len(gated)}"
               if ml is not None else "")
            + f", после правила одной на имя {len(keep)}")
        # Живая позиция держит деньги до ПЛАНОВОГО конца срока, а не до
        # последнего бара записи: касса возвращает маржу по `exit_ts`, и
        # оставив там конец записи, мы вернули бы деньги позиции, которая
        # на бирже открыта. Исход при этом не начисляется — до планового
        # конца очередь до него не доходит, то есть отметка открытой
        # позиции в счёт книги не попадает.
        plan = [(dict(r, exit_ts=float(r.get("sched_end") or r["exit_ts"]))
                 if r.get("state") in ("open", "cut") else r)
                for r in keep]
        for dep in R.DEPOSITS:
            rows = []
            c = D6.ration(plan, R.share(dep, rk), deposit=dep,
                          min_notional=R.MIN_NOTIONAL, keep_rows=rows)
            c["slots"] = R.slots(dep, rk)
            op, cut = [], []
            for (r, margin) in rows:
                st = r.get("state", "closed")
                if st == "closed":
                    out.append({
                        "dep": int(dep), "ruler": rk, "at": float(r["at"]),
                        # сторона — ЗАПИСЬ факта из самой симуляции, а не
                        # вывод из ключа книги при чтении: до этого поля
                        # показ приписывал позициям короткой книги лонг,
                        # и владелец спросил, откуда в шорт-книге лонги
                        "side": r.get("side") or R.side_of(rk),
                        "exit_ts": float(r["exit_ts"]), "sym": r["sym"],
                        "lev": round(float(r["lev"]), 3),
                        "margin": round(float(margin), 4),
                        "pnl_frac": round(float(r["pnl"]), 6),
                        "usd": round(float(r["pnl"]) * float(margin), 4),
                        "exit": r.get("exit"),
                        # исход посчитан по КОТИРОВКЕ в хвосте, а не по
                        # принтам: пометка едет с самой сделкой, иначе
                        # деньги по котировке потом не отделить
                        "tail": r.get("tail"),
                        # цены позиции и её входы: без них позиция не
                        # разворачивается, а средняя ТВХ неоткуда взяться
                        "entry_px": r.get("entry_px"),
                        "exit_px": r.get("exit_px"), "avg": r.get("avg"),
                        "depth": r.get("depth"),
                        "fills": r.get("fills"),
                        # обещание модели: из него выводится уровень цели,
                        # и он ступенчатый — якорь у цели плавающая ТВХ
                        "fav_bp": r.get("fav_bp"),
                        "written_at": now, "rules": R.RULES})
                    continue
                item = {"sym": r["sym"], "at": float(r["at"]),
                        "side": r.get("side") or R.side_of(rk),
                        "lev": round(float(r["lev"]), 3),
                        "margin": round(float(margin), 4),
                        # отметка, а не исход: позиция ещё живёт
                        "mark_frac": round(float(r["pnl"]), 6),
                        "mark_usd": round(float(r["pnl"]) * float(margin), 4),
                        "entry_px": r.get("entry_px"), "avg": r.get("avg"),
                        "depth": r.get("depth"), "fills": r.get("fills"),
                        "fav_bp": r.get("fav_bp"),
                        "last_ts": float(r.get("end_ts") or 0.0),
                        "sched_end": float(r.get("sched_end") or 0.0),
                        # Почему позиция осталась ОБОРВАННОЙ, когда
                        # правило хвоста уже применено (`tail.apply`).
                        # Без причины «оборванных N» читается как «хвост
                        # не работает», а причин две разных и лечатся
                        # они разным: книги в хвосте нет вовсе (символ
                        # перестали писать) либо книга кончилась раньше
                        # планового конца.
                        "cut_why": r.get("cut_why"),
                        "book_end": r.get("book_end"),
                        "state": st}
                (op if st == "open" else cut).append(item)
            c["open_n"], c["cut_n"] = len(op), len(cut)
            cells[_cell(rk, dep)] = c
            live[_cell(rk, dep)] = {
                "positions": op, "cut": cut,
                "mark_usd": round(sum(x["mark_usd"] for x in op), 2),
                "priced": len(op), "at": now}
            log(f"  депозит ${dep:,.0f}: мест {c['slots']}, "
                f"взято {c['taken']}, нет кассы {c['no_cash']}, "
                f"мельче ${R.MIN_NOTIONAL:g} {c['too_small']}, "
                f"открытых {len(op)}, оборванных записью {len(cut)}")
    return out, cells, one, live


def append_journal(rows, path=None, log=print):
    """Дописывает только НОВЫЕ решения. Запись write-ahead: строка,
    однажды попавшая в журнал, не переписывается — иначе момент записи
    можно было бы подвинуть, и «вперёд» перестало бы что-то значить.

    Путь разрешается В МОМЕНТ ВЫЗОВА. Прежде он стоял значением по
    умолчанию, то есть замерзал на импорте: прогон с подменённым
    `R.JOURNAL` (тест, демо) молча дописывал бы в НАСТОЯЩУЮ запись
    книги — а журнал есть единственное здесь невосстановимое. Тот же
    класс, что замороженный на импорте путь склада в Z2/Z3, и он уже
    выстрелил: первый прогон проверки кэша положил подставные строки в
    живой журнал.
    """
    path = path or R.JOURNAL
    old, bad = R.read_journal(path)
    # Ключ дедупа несёт ЛИНЕЙКУ: одно решение живёт в обеих книгах, и без
    # неё вторая книга целиком читалась бы повтором первой и не писалась
    # бы никогда.
    # В ключ входит и ВЕРСИЯ ПРАВИЛ: строка, писанная другим билетом,
    # той же строкой не является, и без версии смена правил не дописала
    # бы ни одной записи — книга новых правил осталась бы пустой.
    seen = {R.journal_key(r) for r in old}
    fresh = [r for r in rows if R.journal_key(r) not in seen]
    # Ротация СУТОЧНАЯ, и она не про порядок: журнал одним файлом
    # вырос до 11 МБ и упёрся в защиту от опасного коммита (5 МБ),
    # заморозив ВЕСЬ канал публикации на шестнадцать часов. Файл
    # выбирается по МЕТКЕ РЕШЕНИЯ, а не по «сегодня»: пересчёт по
    # прошлому обязан лежать своей датой.
    # Часть суток тоже ограничена по РАЗМЕРУ: одно решение живёт во всех
    # книгах разом, и суточные куски 20–21 августа доросли до 4.2–4.35 МБ
    # при защите коммита в 5 МиБ. Пересчёт истории перешагнул бы порог и
    # заморозил весь канал публикации — это уже стоило шестнадцати часов
    # тишины. Раскладку по частям считает `R.shard_place`: она знает
    # размеры на диске, а прогон только пишет.
    days = {}
    for r in fresh:
        ln = json.dumps(r, ensure_ascii=False) + "\n"
        days.setdefault(R.shard_day(r.get("at")), []).append(ln)
    shards = {}
    for d in sorted(days):
        shards.update(R.shard_place(path, d, days[d]))
    if fresh:
        os.makedirs(os.path.dirname(path), exist_ok=True)
        for sh in sorted(shards):
            with open(sh, "a", encoding="utf-8") as f:
                f.write("".join(shards[sh]))
    log(f"журнал: было {len(old)}, дописано {len(fresh)}"
        + (f" в {len(shards)} файлов записи" if shards else "")
        + (f", битых строк {bad}" if bad else ""))
    return {"had": len(old), "added": len(fresh), "bad": bad,
            "shards": len(shards)}


def _stats(rows, deposit):
    """Итог, просадка и форма по дням — на ЭТОМ подмножестве строк."""
    if not rows:
        return None
    day, cnt, bt = {}, {}, {}
    for r in rows:
        d = time.strftime("%Y-%m-%d", time.gmtime(float(r["exit_ts"])))
        day[d] = day.get(d, 0.0) + float(r["usd"])
        cnt[d] = cnt.get(d, 0) + 1
        # Сколько строк дня — пересчёт по прошлому. Предикат ТОТ ЖЕ, что
        # делит книгу (`R.ahead`), а не его копия: разойдись они, день
        # в таблице был бы помечен не тем, чем помечен в своде.
        if R.ahead(r.get("at"), r.get("written_at")) is not True:
            bt[d] = bt.get(d, 0) + 1
    ks = sorted(day)
    v = np.array([day[k] for k in ks], dtype=float)
    eq = float(deposit) + np.cumsum(v)
    dd = float(np.min(eq / np.maximum.accumulate(eq) - 1.0))
    pos = [x for x in v if x > 0]
    # Доля прибыльных ПОЗИЦИЙ и среднее время в сделке. Ничья (ровно
    # ноль) прибыльной не считается — конвенция проекта разрешает её не
    # в свою пользу. Время идёт от первого рунга до выхода: у лестницы
    # доливы живут ВНУТРИ позиции, и считать их отдельными сделками
    # значило бы мерить не то, чем книга торгует.
    wins = sum(1 for r in rows if float(r["usd"]) > 0)
    holds = [(float(r["exit_ts"]) - float(r["at"])) / 3600.0 for r in rows
             if r.get("exit_ts") is not None and r.get("at") is not None]
    best = max(rows, key=lambda r: float(r["usd"]))
    wo = sum(float(r["usd"]) for r in rows if r["sym"] != best["sym"])
    # Концентрация по ДНЯМ — не то же, что по именам: один рыночный
    # эпизод раздаёт деньги десяткам имён сразу, и колонка «без лучшего
    # имени» его не видит. Три дня из трёх вычитать нечего, поэтому там
    # величина НЕ измерена, а не равна нулю.
    top3 = sorted(v, reverse=True)[:3]
    wo3d = (round(float(v.sum() - sum(top3)), 2) if len(ks) > 3 else None)
    return {
        "n": len(rows), "days": len(ks),
        "usd": round(float(v.sum()), 2),
        "final": round(float(v.sum()) / float(deposit), 4),
        "max_dd": round(dd, 4),
        "day_median": round(float(np.median(v)) / float(deposit), 5),
        "day_worst": round(float(np.min(v)) / float(deposit), 4),
        "day_green": round(float(np.mean(v > 0)), 3),
        # Доля прибыльных позиций и среднее время в сделке: день и
        # позиция — разные знаменатели, и «зелёных дней» на вопрос
        # «сколько сделок в плюс» не отвечает.
        "win": round(wins / len(rows), 3),
        "hold_h": (round(float(np.mean(holds)), 1) if holds else None),
        "hold_med_h": (round(float(np.median(holds)), 1) if holds else None),
        # укус: |худший день| / медиана прибыльного дня (мера устойчивости)
        "bite": (round(abs(float(np.min(v))) / float(np.median(pos)), 1)
                 if pos and float(np.median(pos)) > 0 else None),
        "top_sym": best["sym"],
        "usd_wo_top": round(wo, 2),
        "top_day": ks[int(np.argmax(v))],
        "usd_wo_top3d": wo3d,
        "names": len({r["sym"] for r in rows}),
        # Входов больше, чем позиций: позиция есть лестница, и каждый её
        # долив — свой вход. Считаются числом, чтобы «позиций» и «сделок»
        # нельзя было спутать.
        "fills": sum(len(r.get("fills") or []) or 1 for r in rows),
        # Разбивка по суткам: итог по книге отвечает «сколько всего» и
        # молчит о том, КОГДА — сумма за месяц может стоять на одном дне.
        "days_rows": [{"d": k, "usd": round(float(day[k]), 2),
                       "n": int(cnt.get(k, 0)),
                       "bt": int(bt.get(k, 0))} for k in ks],
        "n_bt": int(sum(bt.values())),
        # Сколько сделок книги закрыто по КОТИРОВКЕ (хвост ленты
        # продолжен серединой стакана, `tail.py`). Считается там же, где
        # остальные числа книги: вторая реализация на странице разошлась
        # бы с этой, а величина отвечает на вопрос «чем посчитаны деньги»
        # и молчать о ней нельзя.
        "n_tail": int(sum(1 for r in rows if r.get("tail"))),
    }


def summarize(path=None, live=None):
    """Свод по книгам: ОДНА кривая, и в ней помечено, что бэктест.

    Решение владельца 2026-09-04: бэктест и live не разделять, а вести
    одним счётом — у сделки бэктеста метка. Кривая книги оттого и
    непрерывна: пересчёт по прошлому есть предыстория живой записи, а не
    вторая книга. Числа обеих групп при этом стоят рядом отдельно, и это
    не половинчатость: веса модели видели те часы, и без объёма бэктеста
    общая кривая читалась бы как трек.

    Книга есть пара «режим × депозит», и ключ свода несёт обе: склеив их
    по депозиту, мы сложили бы две книги в одну кривую.

    `live` — открытые позиции счётного прогона по ключу книги. В журнал
    они не идут НИКОГДА: журнал есть запись случившегося и дописывается,
    а отметка открытой позиции меняется каждый час. Их нет вовсе —
    значит числа даёт пересборка, и «открытых нет» отличается от «мы не
    считали» полем `live_known`.
    """
    rows, bad = R.read_journal(path or R.JOURNAL)
    live = live or {}
    out = {"bad_lines": bad, "books": {},
           "rulers": list(R.RULER_ORDER), "deposits": list(R.DEPOSITS)}
    for rk in R.RULER_ORDER:
        for dep in R.DEPOSITS:
            key = _cell(rk, dep)
            mine = [r for r in rows if int(r.get("dep", 0)) == int(dep)
                    and R.is_current(r)
                    and R.ruler_of(r) == rk]
            fwd, back = R.split_rows(mine)
            op = live.get(key)
            b = {"deposit": dep, "ruler": rk, "ruler_title": R.ruler_title(rk),
                 "slots": R.slots(dep, rk),
                 "ticket": R.ticket(dep, rk),
                 "all": _stats(mine, dep),
                 "forward": _stats(fwd, dep), "restored": _stats(back, dep),
                 "n_forward": len(fwd), "n_restored": len(back),
                 "live_known": op is not None}
            if op is not None:
                b["open"] = op
            out["books"][key] = b
    return out


def _pct(x, d=2):
    return "—" if x is None else f"{x * 100:+.{d}f} %"


def _tail_words(s):
    """Числа хвоста словами. Нет чисел — так и сказано, а не ноль.

    `--restat` ничего не считает; если в артефакте чисел нет вовсе (он
    прежнего образца), фраза обязана назвать причину — ноль здесь читался
    бы как «хвост не сработал ни разу».
    """
    t = s.get("tail")
    if not isinstance(t, dict) or not t:
        return ("Чисел этого прогона нет: их даёт только счётный прогон, "
                "а этот отчёт пересобран из журнала.")
    out = (f"На этом прогоне хвост дописан {t.get('minutes', 0)} минут "
           f"{t.get('syms', 0)} именам; исходов, посчитанных по котировке, "
           f"{t.get('closed_tail', 0)} из {t.get('closed_all', 0)} закрытых "
           f"позиций книг; вход из котировки отклонён у "
           f"{t.get('entry_dropped', 0)} решений; имён, у которых хвоста "
           f"нет и в книге, {t.get('dry', 0)} — такая позиция остаётся "
           f"оборванной, потому что отсутствие свидетельства не есть "
           f"свидетельство отсутствия.")
    w = t.get("cut_why")
    if isinstance(w, dict) and w:
        # Причины считаются по ЗАПИСЯМ (одно решение живёт в обеих
        # линейках), как и «исходов по котировке» рядом, — иначе два
        # числа одного прогона мерились бы в разных единицах.
        out += (" Оборванными остались записи по причинам: "
                + "; ".join(f"{k} — {v}" for k, v in
                            sorted(w.items(), key=lambda kv: -kv[1]))
                + ".")
    if t.get("zero_vol_tape"):
        out += (f" ВНИМАНИЕ: баров ЛЕНТЫ с нулевым объёмом "
                f"{t['zero_vol_tape']} — признак «принт был» перестал "
                f"различать, и правило лимитки надо перепроверить.")
    return out


def report(s):
    def tks(rk):
        return " / ".join(f"${R.ticket(d, rk):g}" for d in R.DEPOSITS)

    def sls(rk):
        return " / ".join(str(R.slots(d, rk)) for d in R.DEPOSITS)

    L = ["# DCA — бумажные книги на трёх депозитах", "",
         "Книг девять: три РЕЖИМА × три депозита. Внутри режима книги "
         "отличаются РОВНО депозитом: правила, гейты, ограда и "
         "выходы у них одни. Билет при этом НЕ один и тот же, и это не "
         "третья ось: он считается ОДНОЙ формулой — "
         "`максимум(пол режима, депозит / (пик режима × запас))`, — "
         "просто пол и пик у каждого режима свои.", "",
         "| режим | пол | пик | билет | мест |",
         "|---|--:|--:|--:|--:|"]
    for rk in R.RULER_ORDER:
        L.append(f"| {R.ruler_title(rk)} | ${R.floor_of(rk):g} | "
                 f"{R.peak_of(rk)} | {tks(rk)} | {sls(rk)} |")
    L += ["",
          "**Пол задаёт биржа, и он у режимов разный.** Минимальный "
          f"ордер площадки ровно ${R.MIN_NOTIONAL:g}, самый мелкий рунг "
          f"лестницы — {R.RUNG_SHARE:g} нотионала, значит маржа не бывает "
          "меньше `$5 / 0.25 / плечо`. Худшее плечо режима без гейта есть "
          "1× (забор выдаёт от единицы) — отсюда $20 и, с четвертью "
          f"запаса на просадку, пол ${R.TICKET_MIN:g}. У режима с гейтом "
          f"худший случай есть сам гейт ({R.AGGR_MIN_LEV:g}×), и пол "
          f"выходит вчетверо ниже — ${R.floor_of('aggr'):g}.", "",
          "**Потолок задаёт книга (решение владельца 2026-09-04): билет "
          "поднят так, чтобы хватало на ВСЕ места.** Пики ИЗМЕРЕНЫ по "
          "журналу: у двух режимов без гейта он один и тот же "
          f"({R.PEAKS['safe']} — они берут одни решения и различаются "
          f"лишь плечом), у режима с гейтом {R.PEAKS['aggr']}. Запас "
          f"{R.PEAK_MARGIN:g}× нужен потому, что пик есть максимум "
          "выборки, а сечение растёт с записью. Правило само говорит "
          "главное: **мелкий депозит наполнить нельзя** — у него "
          "связывает пол биржи.", "",
          "**Что здесь изменено 2026-09-04 и почему** (просьба владельца "
          "«оптимизировать книги под их названия»). Прежде на все три "
          "режима шёл пик ПУЛА, и «агрессивная» от этого стояла "
          "недогруженной: позиций у неё вдвое меньше, то есть её "
          "собственный пик ниже, и при полном по её меркам портфеле часть "
          "денег простаивала. Замер `name_check.py` назвал это числом — "
          "загрузка 4.0 % против 19.5 у «оптимальной», пик 59 % против "
          "100, — то есть имя было честным по позиции и нечестным по "
          "книге. **Билет при этом не стал особым:** формула одна на все "
          "режимы, различаются лишь их собственные пол и пик, поэтому "
          "режимы по-прежнему отличаются РОВНО одним объявленным "
          "правилом (гейтом плеча), и контрольная рука со старым билетом "
          "не нужна. Числа безопасной и оптимальной не сдвинулись ни на "
          "цент — их пол и пик прежние.", "",
          "**Чего правка НЕ чинит, и это надо знать до чтения таблицы.** "
          "Билет лечит простой денег при полном портфеле; он не лечит "
          "того, что режим с гейтом торгует вспышками — на нашей записи "
          "13 календарных дней из 26. Средняя загрузка у него останется "
          "заметно ниже: полный по своим меркам портфель он держит редко. "
          "Это свойство ГЕЙТА, а не недогруз по нашему выбору, и лечить "
          "его билетом нельзя — билет, поднятый до средней загрузки "
          "соседей, в пик потребовал бы больше денег, чем есть, и состав "
          "книги начал бы решаться очередью за кассой, а не правилом.", "",
          "**Биржевое правило соблюдается с первого дня:** у имени позиция "
          "одна, второй выбор по той же монете пропущен. Реплей D-серии "
          "этого не делал, и его «пик 3206» описывал ЛОТЫ, а не позиции.",
          "",
          "**Кривая одна: бэктест и live в общем счёте** (решение "
          "владельца 2026-09-04). Пересчёт по прошлому есть ПРЕДЫСТОРИЯ "
          "живой записи, а не вторая книга, и рвать её надвое незачем — "
          "у сделки бэктеста стоит метка. Решение считается записанным "
          f"вперёд, если попало в журнал не позже {R.AHEAD_H} ч после "
          f"самого себя (предел жизни позиции {R.HOLD_H} ч плюс двое "
          "суток на прогон). Числа обеих групп стоят рядом отдельной "
          "таблицей — и это не половинчатость: веса модели видели те "
          "часы, и без объёма бэктеста общая кривая читалась бы треком.",
          "",
          "**Считаются ПОЗИЦИИ, а не сделки.** У имени позиция одна, а "
          "входов у неё несколько: база плюс доливы лестницы. Поэтому в "
          "таблице стоит число закрытых позиций, рядом число входов, а "
          "открытые позиции идут ОТДЕЛЬНОЙ строкой с отметкой — до "
          "выхода их результат станет любым, и складывать его с "
          "закрытым нельзя.", "",
          f"**Версия правил книг — {R.RULES}, и запись начата с неё.** "
          "Смена версии отставляет прежние строки не из аккуратности: "
          "правило, по которому они писаны, было другим, и сшивать две "
          "кривые в одну значит показывать книгу, которой не было. "
          "Версия поднята трижды: билетом (пол режима и его же пик), "
          "исходом позиции, чей срок ещё шёл (ниже), и ТЕЙКОМ.", "",
          f"**Тейк (правка 2026-09-05, замер D8): цель от плавающей ТВХ, "
          f"расстояние {R.TAKE_MULT:g} обещания `mfe`.** Было — "
          "неподвижная цена `вход · (1 + mfe)`, тогда как ТВХ едет вниз "
          "с каждым доливом. Оба множителя выведены сеткой из 17 "
          "объявленных до прогона ячеек (`dca_ladder/out/D8-take-1m.md`): "
          "якорь берётся за согласованность (тейк по ТВХ платит "
          "тождественно «заполненный нотионал × доля»), расстояние — за "
          "деньги и форму (обещание стояло на 2.56 % при суточной σ "
          "имени 5.55 %). Вся прежняя история пересчитана этим правилом "
          "и лежит В ТОМ ЖЕ блоке с пометкой бэктеста — решение "
          "владельца: одна кривая, у сделки пересчёта метка.", "",
          f"**Решение, принятое до {time.strftime('%Y-%m-%d %H:%M', time.gmtime(R.RULES_SINCE))} "
          "UTC, помечено бэктестом по построению** — правило выбрано "
          "ПОСЛЕ того, как эти часы стали видны, и задержка записи тут "
          "ничего не меняет. Без этой границы каждая смена правил молча "
          "перекрашивала бы последние "
          f"{R.AHEAD_H} ч пересчёта в «вперёд».", "",
          "**Позиция, чей срок ещё идёт, больше не закрывается «по "
          "сроку».** Это дефект, найденный при этой правке: реплей "
          "кончал позицию последним баром записи и выдавал это за исход "
          "правила — тот же класс, что D7 нашла у обрыва ряда. Теперь "
          "«срок» засчитывается, только если окно дошло до планового "
          "конца; иначе позиция либо ЖИВА (её символ пишется до сих "
          "пор), либо оборвана записью (ряд символа кончился раньше "
          "остальных), и вторая в счёт не идёт вовсе.", "",
          "**Хвост ленты продолжается серединой стакана (решение "
          "владельца 2026-09-05), и это ПРАВИЛО книги, а не поправка.** "
          "Бары исходов читаются из ленты, а минута без единой сделки "
          "бара не даёт вовсе: у тонкого имени хвост окна пуст не "
          "потому, что цены не было, а потому, что по ней не торговали — "
          "стакан в те же минуты снимается раз в секунду. Позиция, чьё "
          "окно не дошло до конца только из-за отсутствия принтов, "
          "теперь получает исход по НАБЛЮДЁННЫМ ценам вместо вечного "
          "«оборвано записью». Правило держит три границы: дописывается "
          "только хвост (внутренние дыры не заполняются — по тем же "
          "барам считаются вход и цены рунгов); вход из котировки "
          "позицию не открывает (хвост продолжает начатое, а не заводит "
          "новых сделок); лимитка на минуте без принтов не заполняется "
          "вовсе — рунг и тейк исполняет чужой принт, а пол капитуляции, "
          "ликвидация и срок суть рыночные выходы против котировки и "
          "считаются. " + _tail_words(s) + " **Архива записи это не "
          "потребовало:** в журнал попадают только закрытые позиции, а "
          "закрытой позиция становится, когда её окно дошло до конца "
          "ЛЕНТОЙ, то есть хвост лежит вне её окна; оборванные же не "
          "писались вовсе. Значит правило не меняет ни одной записанной "
          "строки, а только добавляет исходы, которых не было. **Деньги "
          "от него ХУДЕЮТ, и это ожидалось**: замер `DCA-cut-check.md` "
          "дал −8.9…−121.2 $ по книгам (−0.05…−1.26 п.п. к депозиту) — "
          "оборванные позиции были в среднем хуже средней, и прежние "
          "числа книг слегка льстили.", "",
          "**Две колонки концентрации отвечают на РАЗНЫЕ вопросы.** «Без "
          "лучшего имени» ловит одну разогнанную монету; «без 3 лучших "
          "дней» ловит один рыночный эпизод, который раздаёт деньги "
          "десяткам имён разом, и первой колонке он невидим. Если вторая "
          "уводит итог в минус, книга описывает не правило, а тот эпизод. "
          "У книги моложе четырёх дней вычитать нечего, и там стоит "
          "прочерк — величина не измерена, а не равна нулю.", "",
          "**Режимов три, и книг поэтому девять.** Плечо не настройка "
          "агрессивности: оно выводится из неравенства безопасности, и "
          "режим задаёт, ИЗ ЧЕГО. "
          + " ".join(f"**{v['title'].capitalize()}** — {v['plain']}"
                     for k, v in ((k, R.RULERS[k]) for k in R.RULER_ORDER))
          + " Имена — ярлыки, а не вердикт: какой режим лучше, покажет "
          "форвард, и все три ведутся параллельно ровно затем, чтобы "
          "вопрос решали числа, а не выбор задним числом.", ""]
    for name, key in (("Общий счёт: бэктест и live вместе", "all"),
                      ("Из него записано вперёд (live)", "forward"),
                      ("Из него пересчёт по прошлому (бэктест)", "restored")):
        L += [f"## {name}", "",
              "| режим | депозит | билет | мест | позиций | входов | "
              "имён | дней | "
              "$ | к депозиту | просадка | медиана дня | худший день | "
              "зелёных | укус | $ без лучшего имени | $ без 3 лучших дней |",
              "|---|---|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|--:|"
              "--:|--:|--:|"]
        # порядок «депозит, внутри линейки» ставит пару рядом: сравнивают
        # линейки при ОДНОМ депозите, а не депозиты при одной линейке
        for dep in R.DEPOSITS:
            for rk in R.RULER_ORDER:
                b = (s.get("books") or {}).get(_cell(rk, dep)) or {}
                nm, st = R.ruler_title(rk), b.get(key)
                if not st:
                    L.append(f"| {nm} | ${dep:,.0f} | "
                             f"${R.ticket(dep, rk):g} | "
                             f"{b.get('slots', '—')} | 0 | — | — | — | "
                             "— | — | — | — | — | — | — | — | — |")
                    continue
                L.append(
                    f"| {nm} | ${dep:,.0f} | "
                    f"${b.get('ticket', R.ticket(dep, rk)):g} | "
                    f"{b['slots']} | {st['n']} | "
                    f"{st.get('fills', st['n'])} | "
                    f"{st['names']} | {st['days']} | {st['usd']:,.2f} | "
                    f"{_pct(st['final'])} | {_pct(st['max_dd'])} | "
                    f"{_pct(st['day_median'], 3)} | "
                    f"{_pct(st['day_worst'])} | {st['day_green']:.2f} | "
                    f"{'—' if st['bite'] is None else st['bite']} | "
                    f"{st['usd_wo_top']:,.2f} | "
                    + ("—" if st.get("usd_wo_top3d") is None
                       else f"{st['usd_wo_top3d']:,.2f}") + " |")
        L.append("")
        if key == "forward" and all(
                not ((s.get("books") or {}).get(_cell(rk, d)) or {}).get(key)
                for d in R.DEPOSITS for rk in R.RULER_ORDER):
            L += ["Наблюдения ещё нет ни у одной книги, и это не пустота "
                  "показа: журнал начат сегодня, а решение попадает сюда "
                  "только после того, как его позиция закрылась. Первые "
                  "строки появятся следующим суточным прогоном.", ""]
    # --- открытые позиции: отметка, а не исход ---------------------------
    books = s.get("books") or {}
    known = [k for k in books if books[k].get("live_known")]
    L += ["## Открытые позиции", ""]
    if not known:
        L += ["Их числа даёт только счётный прогон: отметка живой позиции "
              "меняется каждый час, поэтому в журнал она не идёт никогда, "
              "а этот прогон был пересборкой свода. Прочерка тут нет — "
              "величина не потеряна, её просто не считали сегодня.", ""]
    else:
        L += ["Отметка по последнему бару записи, а не исход: до выхода "
              "результат станет любым. С закрытым счётом она не "
              "складывается нигде. «Оборвана записью» — позиция, чей ряд "
              "кончился раньше остальных (снятие с торгов либо дыра "
              "записи): исхода у неё нет, и в счёт она не идёт.", "",
              "| режим | депозит | открытых | отметка $ | к депозиту | "
              "оборвано записью |", "|---|---|--:|--:|--:|--:|"]
        for dep in R.DEPOSITS:
            for rk in R.RULER_ORDER:
                b = books.get(_cell(rk, dep)) or {}
                op = b.get("open")
                if not op:
                    continue
                n = len(op.get("positions") or [])
                mk = float(op.get("mark_usd") or 0.0)
                L.append(f"| {R.ruler_title(rk)} | ${dep:,.0f} | {n} | "
                         f"{mk:,.2f} | {_pct(mk / float(dep))} | "
                         f"{len(op.get('cut') or [])} |")
        L.append("")
    # --- разбивка по суткам: средний депозит, три режима рядом ----------
    mid = R.DEPOSITS[len(R.DEPOSITS) // 2]
    rows_by = {}
    for rk in R.RULER_ORDER:
        st = ((books.get(_cell(rk, mid)) or {}).get("all") or {})
        for d in (st.get("days_rows") or []):
            rows_by.setdefault(d["d"], {})[rk] = d
    if rows_by:
        L += [f"## По суткам, депозит ${mid:,.0f}", "",
              "Итог книги отвечает «сколько всего» и молчит о том, КОГДА: "
              "сумма за месяц может стоять на одном дне. Здесь общий счёт "
              "(бэктест и live вместе).", "",
              "| сутки | " + " | ".join(R.ruler_title(rk)
                                        for rk in R.RULER_ORDER) + " |",
              "|---|" + "--:|" * len(R.RULER_ORDER)]
        for d in sorted(rows_by):
            cells_ = []
            for rk in R.RULER_ORDER:
                x = rows_by[d].get(rk)
                cells_.append("—" if not x
                              else f"{x['usd']:,.2f} ({x['n']})")
            L.append(f"| {d} | " + " | ".join(cells_) + " |")
        L += ["", "В скобках — число закрытых позиций этого дня.", ""]
    one = s.get("one_name") or {}
    if one:
        L += ["## Гейт плеча и собственный пик режима", "",
              "Гейт стоит ПЕРВЫМ, до правила одной на имя: у режима с "
              "гейтом низкоплечевой ранний вход не случается вовсе, и имя "
              "остаётся свободным для рычажного. Пик считается по тому, "
              "что режим ВЗЯЛ, и стоит рядом с объявленным — тем, из "
              "которого посчитан билет. **Объявленный обязан быть не ниже "
              "измеренного:** ниже означает, что билет велик и книга "
              "теряет решения по кассе, и такая строка кричит, а не "
              "молчит.", "",
              "| режим | решений | отсеял гейт | после одной на имя | "
              "медиана плеча | свой пик позиций | объявлен |",
              "|---|--:|--:|--:|--:|--:|--:|"]
        for rk in R.RULER_ORDER:
            o = one.get(rk)
            if not o:
                continue
            ml = o.get("min_lev")
            L.append(
                f"| {R.ruler_title(rk)} | {o.get('positions', '—')} | "
                + ("нет гейта" if ml is None
                   else f"{o.get('gate_dropped', '—')} (плечо < {ml:g}×)")
                + f" | {o.get('kept', '—')} | "
                + ("—" if o.get("lev_median") is None
                   else f"{o['lev_median']:g}×") + " | "
                + ("—" if o.get("peak_names") is None
                   else str(o["peak_names"])) + " | "
                + ("—" if o.get("peak_declared") is None
                   else str(o["peak_declared"]))
                + (" ⚠ ниже измеренного" if o.get("peak_over") else "")
                + " |")
        over = [rk for rk in R.RULER_ORDER
                if (one.get(rk) or {}).get("peak_over")]
        L.append("")
        if over:
            L += ["**Объявленный пик ниже измеренного у режимов: "
                  + ", ".join(R.ruler_title(rk) for rk in over)
                  + ".** Значит билет посчитан от заниженного пика и "
                  "велик: книга берёт не все свои решения, а первые по "
                  "очереди за кассой, то есть отказ меняет и состав. "
                  "Число в `rules.PEAKS` объявлено до прогона и здесь не "
                  "правится — правка есть решение владельца.", ""]
    cells = s.get("cells") or {}
    L += ["## Что связывает депозит", ""]
    if not cells:
        L += ["Числа отказов даёт только счётный прогон, а этот был "
              "пересборкой свода из журнала. Прочерка тут нет: величины "
              "не потеряны, их просто не считали сегодня.", ""]
    else:
        L += [f"Числа счётного прогона {s.get('computed_at') or ''}. "
              "Касса раздаёт деньги по очереди решений, поэтому книга, "
              "которой не хватает мест, берёт не лучшие сигналы, а "
              "первые: отказ по кассе меняет не только объём, но и "
              "СОСТАВ.", "",
              "| режим | депозит | мест | взято | нет кассы | "
              "мельче пола |", "|---|---|--:|--:|--:|--:|"]
        for dep in R.DEPOSITS:
            for rk in R.RULER_ORDER:
                c = cells.get(_cell(rk, dep))
                if not c:
                    continue
                L.append(f"| {R.ruler_title(rk)} | ${dep:,.0f} | "
                         f"{c.get('slots', '—')} | {c.get('taken', '—')} | "
                         f"{c.get('no_cash', '—')} | "
                         f"{c.get('too_small', '—')} |")
        L.append("")
    L += ["## Чего эти числа НЕ описывают", "",
          "Живого исполнения здесь нет: сделки считаются реплеем по барам "
          "записи, а не сканером на живой цене. Значит не моделируются "
          "проскальзывание, очередь в стакане и задержка входа; долив "
          "лотов в одно имя запрещён правилом, а не сведён в позицию, то "
          "есть маржа и цена ликвидации по-прежнему считаются по каждой "
          "позиции отдельно — на бирже они считаются по слитой. Веса "
          "модели видели эти часы, поэтому пересчёт читается как оценка "
          "СВЕРХУ. Период один и режим рынка один.", ""]
    return "\n".join(L)


def publish(name):
    sh = os.path.join(ROOT, "tools", "publish.sh")
    if os.path.exists(sh):
        subprocess.run(["bash", sh, name], check=False)


def main():
    ap = argparse.ArgumentParser()
    ap.add_argument("--limit", type=int, default=None)
    ap.add_argument("--no-publish", action="store_true")
    ap.add_argument("--full", action="store_true",
                    help="не брать кэш реплея, пересчитать всё заново")
    ap.add_argument("--restat", action="store_true",
                    help="пересобрать свод и отчёт из журнала, не считая")
    a = ap.parse_args()
    os.makedirs(R.OUT, exist_ok=True)
    t0 = time.time()
    extra, live = {}, None
    if a.restat:
        # Пересборка ничего не считает, но и не вправе ТЕРЯТЬ числа
        # счётного прогона: отказы кассы, окно решений и число позиций
        # описывают тот же журнал. Переносятся с меткой ТОГО прогона —
        # иначе они читались бы сегодняшними.
        try:
            with open(R.ARTIFACT, encoding="utf-8") as f:
                was = json.load(f)
            extra = {k: was[k] for k in
                     ("positions", "skipped", "window", "cells", "one_name",
                      "states", "data_end", "tail")
                     if k in was}
            # Открытые позиции переносятся ВМЕСТЕ с их меткой времени:
            # отметка живой позиции стареет, и выдать вчерашнюю за
            # сегодняшнюю значило бы соврать о деньгах, которых сейчас
            # столько нет. Возраст печатает страница.
            # `None` означает «не считали», пустой словарь — «считали, и
            # открытых нет». Смешав их, страница показала бы «открытых 0»
            # там, где просто не смотрели.
            live = (was["live"] if isinstance(was.get("live"), dict)
                    else None)
            if extra:
                extra["computed_at"] = was.get("computed_at") or time.strftime(
                    "%Y-%m-%d %H:%M",
                    time.gmtime(os.path.getmtime(R.ARTIFACT)))
        except (OSError, ValueError):
            extra = {}
    if not a.restat:
        keys = list(R.RULER_ORDER)
        # Пары (правило, параметр) у режимов повторяются: «агрессивная»
        # считает ту же глубину, что «оптимальная», и отличается ГЕЙТОМ.
        # Дубль в списке заставил бы дорогой проход дописать исходы в тот
        # же список дважды — то есть удвоил бы записи молча.
        pairs = []
        for k in keys:
            if RULERS[k] not in pairs:
                pairs.append(RULERS[k])
        # --- инкрементальный проход -------------------------------------
        # Дорого здесь чтение баров, а прошлое не меняется: закрытая
        # позиция при тех же правилах даст тот же исход, и считать её
        # заново каждый час незачем. Заново считаются НОВЫЕ решения и все
        # незакрытые — у них отметка меняется, и вчерашняя была бы ложью.
        cache, why = ((({}, "полный прогон по требованию") if a.full
                       else read_cache()))
        if why:
            print(f"кэш реплея не используется: {why}")
        # Обе стороны разом: бары читаются по символу, и второй проход
        # ради коротких книг стоил бы столько же, сколько первый.
        legs = D6.gated_legs(limit=a.limit, side=None)
        need = needs_replay(cache, legs, pairs)
        print(f"решений под гейтом {len(legs)}, в кэше "
              f"{len(cache) // max(1, len(pairs))}, считаю заново {len(need)}")
        # Источник баров — лента, ПРОДОЛЖЕННАЯ серединой стакана после
        # последнего принта (`tail.py`). Второй копии решения нет: исход
        # считает то же ядро, меняется только то, чем оно накормлено в
        # хвосте. Обе границы правила (вход не из котировки, лимитка не
        # заполняется без принта) держат `tail.apply` и `simulate_dca`.
        # журнал чтения не глушится: `read_hour` говорит им о спасении
        # испорченного архива, а такое молчание уже теряло сигнал
        src = TL.TailBars(log=print)
        got = D6.collect_recs(rulers=pairs, legs=legs, src=src,
                              only=[(g["sym"], g["at"]) for g in need])
        tail = dict(src.stats(),
                    **TL.apply(got["recs"], src.last_tape,
                               src.last_book))
        print(f"хвост: имён с дописанным {tail['syms']}, минут "
              f"{tail['minutes']}, исходов по котировке {tail['marked']}, "
              f"вход из котировки отклонён у {tail['entry_dropped']} решений"
              + (f"; баров ЛЕНТЫ с нулевым объёмом {tail['zero_vol_tape']}"
                 if tail["zero_vol_tape"] else ""))
        for pr, lst in got["recs"].items():
            for r in lst:
                cache[(tuple(pr), r["sym"], round(float(r["at"]), 3))] = r
        write_cache(cache)
        by_pair = {tuple(pr): [] for pr in pairs}
        for (pr, _sym, _at), r in cache.items():
            if pr in by_pair:
                by_pair[pr].append(r)
        # Обещание модели у записей ПРЕЖНЕГО образца: кэш их считал, когда
        # поля ещё не было. Берётся из ТОГО ЖЕ списка ног, которым считает
        # реплей, — это не восстановление по исходу, а тот же источник.
        # Без него у старой позиции не было бы уровня цели на графике, и
        # сказать об этом надо числом: молчаливая половина без линии
        # читалась бы как «у книги цели нет».
        fav_of = {}
        for g in legs:
            try:
                fav_of[(g["sym"], round(float(g["at"]), 3))] = float(g["fav"])
            except (KeyError, TypeError, ValueError):
                # Нога без обещания правила цели не даёт вовсе, и падать
                # тут нельзя: добор — удобство, а не условие прогона.
                continue
        no_fav = 0
        for lst in by_pair.values():
            for r in lst:
                if r.get("fav_bp") is None:
                    v = fav_of.get((r["sym"], round(float(r["at"]), 3)))
                    if v is None:
                        no_fav += 1
                    else:
                        r["fav_bp"] = v
        if no_fav:
            print(f"обещание модели не восстановлено у {no_fav} позиций — "
                  "у них не будет линии цели на графике")
        rows, cells, one, live = build_rows(
            {k: by_pair[tuple(RULERS[k])] for k in keys})
        append_journal(rows)
        extra = {"positions": sum(len(v) for v in by_pair.values())
                 // max(1, len(pairs)),
                 "replayed": got["positions"], "skipped": got["skipped"],
                 # окно считается по ВСЕМ решениям, а не по пересчитанным:
                 # инкрементальный прогон иначе объявлял бы окном
                 # последний час записи
                 "window": D6.window(legs), "cells": cells, "one_name": one,
                 "states": got.get("states"), "data_end": got.get("data_end"),
                 "live": live,
                 # Сколько денег книги посчитано по котировке, видно
                 # числом, а не пересказом: закрытые строки с пометкой
                 # считаются здесь же, из тех же строк, что легли в журнал.
                 "tail": dict(tail,
                              closed_tail=sum(1 for r in rows if r.get("tail")),
                              closed_all=len(rows)),
                 "computed_at": time.strftime("%Y-%m-%d %H:%M",
                                              time.gmtime())}
    s = summarize(live=live)
    s.update(extra)
    # Просадка ОДНОВРЕМЕННО ОТКРЫТЫХ живёт в ячейках кассы (она считается
    # по почасовым отметкам, которых в журнале нет), а страница читает
    # свод. Кладётся ТОЛЬКО в общую группу: величина принадлежит всей
    # книге и по «бэктест/вперёд» не делится — поставить её и туда
    # значило бы приписать подмножеству чужое число.
    for k, c in (s.get("cells") or {}).items():
        b = (s.get("books") or {}).get(k)
        if not (isinstance(b, dict) and isinstance(b.get("all"), dict)):
            continue
        if c.get("open_dd") is not None:
            b["all"]["open_dd"] = c["open_dd"]
            b["all"]["open_dd_share"] = c.get("open_dd_share")
    s["secs"] = round(time.time() - t0, 1)
    s["rules"] = {"RULES": R.RULES, "TICKET": R.TICKET_MIN,
                  "TICKET_MIN": R.TICKET_MIN, "PEAK_SEEN": R.PEAK_SEEN,
                  "PEAK_MARGIN": R.PEAK_MARGIN,
                  "TICKETS": {rk: {str(int(d)): R.ticket(d, rk)
                                   for d in R.DEPOSITS}
                              for rk in R.RULER_ORDER},
                  "FLOORS": {rk: R.floor_of(rk) for rk in R.RULER_ORDER},
                  "PEAKS": {rk: R.peak_of(rk) for rk in R.RULER_ORDER},
                  "DEPOSITS": R.DEPOSITS, "AHEAD_H": R.AHEAD_H,
                  "TAKE_ANCHOR": R.TAKE_ANCHOR, "TAKE_MULT": R.TAKE_MULT,
                  "RULES_SINCE": R.RULES_SINCE,
                  "HOLD_H": R.HOLD_H, "ONE_PER_NAME": R.ONE_PER_NAME,
                  "MIN_EDGE_BP": R.MIN_EDGE_BP, "MIN_RR": R.MIN_RR,
                  "SURVIVE_MULT": R.SURVIVE_MULT,
                  "AGGR_MIN_LEV": R.AGGR_MIN_LEV,
                  "FLOOR_FRAC": R.FLOOR_FRAC,
                  "RULERS": {k: dict(R.RULERS[k]) for k in R.RULER_ORDER},
                  "RULER_ORDER": list(R.RULER_ORDER)}
    with open(R.ARTIFACT, "w", encoding="utf-8") as f:
        json.dump(s, f, ensure_ascii=False, indent=1)
    txt = report(s)
    with open(os.path.join(R.OUT, "DCA-paper.md"), "w",
              encoding="utf-8") as f:
        f.write(txt)
    print(txt)
    if not a.no_publish:
        publish("DCA: бумажные книги, три режима × три депозита")


if __name__ == "__main__":
    main()
