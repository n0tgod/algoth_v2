# Сводка владельцу

Собрано 2026-09-02 02:30 UTC. Охват — сутки от прошлой сводки.

## Коротко

Круг за 2026-09-01 прошёл целиком: бриф → заявка → суточный прогон →
потолок → объявление. Заявка предлагающего **закрыта потолком**, в
реестр ушла только контрольная рука (жребий). Вердикта по фабрике нет и
быть не может: календаря **25 суток при требуемых 90**
(`research/factory/out/FACTORY-day-1m.md`).

Одна поломка требует вашего действия — см. последний раздел.

## Что посчитано

Суточный прогон 2026-09-01 18:55 UTC, 226 677 ног
(`research/factory/out/FACTORY-day-1m.md`):

| величина | число |
|---|--:|
| кандидатов в пуле | 6 |
| вылетело | 0 |
| эффективное N | 2.8 |
| средняя связь дневных денег | +0.230 |
| суток записи | 25 из 90 |

Полосы: отобранные — медиана нетто **−267.7 б.п.** (лучшая +485.3,
худшая −647.6), случайная контрольная — медиана **+115.4**, нуль **0.0**.

**Читать это как результат нельзя, и вот почему.** Все шесть книг
считаны задним числом по записи, то есть на данных, которых в момент их
объявления не существовало; кандидаты стоят вокруг одной точки и почти
дублируют друг друга (эффективное N 2.8 при шести номинальных); а
максимум случайной полосы и есть «лучший из шести» под нулём. Сегодня
случайная полоса **выше** отобранной — это диагностика, а не приговор
отбору, но объявлять «отбор работает» на таких числах нельзя.

## Что решено за сутки

**Заявка предлагающего закрыта потолком.** Роль предложила изолировать
одну ось — предел жизни позиции 24 часа против 72 при прочих равных, и
её содержательное утверждение я проверил по коду: оно верно, значения
оси действительно различаются только этим числом. Закрыла заявку
арифметика независимости: эффективное N пула **2.79 без неё и 2.56 с
ней** (`research/factory/out/CEILING-1m.md`). Пятый сосед вокруг той же
точки наблюдений не добавляет — он почти копия уже живой книги.

Практическое следствие записано в бриф: следующая заявка обязана
предъявлять конструкцию, дневные деньги которой слабо связаны с уже
живыми, а не «ещё одну ось при прочих равных».

**Роль предлагающего нашла ошибку в моём же брифе** и отказалась на неё
опираться: заголовочное число про «согласие рук» (+441 б.п., p = 0.000)
принадлежит книге 24 ч, которую фабрика сегодня исполнить не может, а на
исполнимом горизонте 4 ч эффект неотличим от нуля (−3.1 и +42.8 б.п. при
p = 0.50 и 0.15) и переворачивается по половинам истории
(`research/probe_agree/out/AGREE-1m.md`). В сегодняшнем брифе это
исправлено.

**Объявлена одна контрольная рука** — `h4_z_f30_w5_gs_rrhi_se_b0_a1`,
вытянута жребием из пространства целиком
(`research/factory/out/ledger.jsonl`). Это не ставка: контрольная рука
существует, чтобы отобранным было с чем сравниваться.

## Что сломалось

1. **Разведчик не запускался ни разу — шесть попыток, все одинаково.**
   CLI на сервере версии 2.1.220, а объявленная роли модель требует
   2.1.251 или новее (`research/factory/out/agents-runs.jsonl`). Значит
   внешнего входа у фабрики сегодня нет вовсе, и предлагающий работал
   только на измеренном нами.
2. **Прошлый заход брифа отвергнут контрактом** — сводка владельцу не
   несла ни одного указателя на файл, а проверка требует не меньше
   одного (`research/factory/out/cycle-brief.log`). Бриф при этом был
   годен; переписаны оба файла.
3. **Один прогон строителя упал по пределу сессии подписки**
   (`research/factory/out/agents-runs.jsonl`). Работа не потеряна:
   следующий заход прошёл.

## Чего система ждёт

- **Календаря.** Вердикт выносится только вперёд, и до 90 суток записи
  ускорить его нечем. Сегодня 25.
- **Вашего действия — обновить `claude` на сервере до 2.1.251 или
  новее.** Пока этого нет, роль разведчика не работает, и фабрика
  варится в собственных замерах.

Настоящих наблюдений в бумажной месячной книге по-прежнему **0
траншей**: записанное вперёд начнётся, когда закроется первое форвардное
окно (`research/paper_monthly/out/PAPER-30d.md`). Восстановленная задним
числом часть треком не является и в счёт не идёт.
