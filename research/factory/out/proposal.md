# Заявка предлагающего · 2026-09-11 — пустой день

**Вид:** заявки нет (`proposed: false`). **Почему одной строкой:** ни строка пространства, ни своя механика, ни меню разведчика не дают сегодня испытания, которое добавило бы независимое наблюдение и не легло бы седьмым в очередь непрогнанных. Ниже — что проверено, с числами и указателями, и что именно откроет заявку завтра.

Пустой день — законный ответ роли: объявленное испытание занимает слот на месяцы и ухудшает поправку всем остальным. Заявка ради галочки хуже отказа.

## 1. Строка пространства: независимой строки на листе 4 ч нет

**Что исполнимо.** 1296 сочетаний из 5184, и все на цели `fwd_4h` (`research/factory/out/FACTORY-day-1m.md`). Цель 24 ч и «корзина целиком» неисполнимы полями листа (`research/factory/space.py`, `unavailable`).

**Что режет новичка.** Пул — 7 книг, средняя связь дневных денег +0.135, эффективное N 3.9. Формула пула — `N / (1 + (N−1)·r̄)` (`research/factory/ledger.py`, `effective_n`): 7 / (1 + 6·0.135) = 3.87. Восьмая книга поднимает N, только если её средняя связь с семью ниже ≈ 0.20. Арифметика для проверки: 8 / (1 + 7·r̄′) > 3.87 ⇒ r̄′ < 0.152 ⇒ (21·0.135 + 7·c) / 28 < 0.152 ⇒ c < 0.205. Последний параметрический сосед закрыт именно так: 2.79 → 2.56 при связи 0.761 с теснейшей книгой (`research/factory/out/ceilings.jsonl`).

**Где в пространстве независимость.** Четыре отобранные книги — соседи по одной оси (`h4_z_f30_w5_gl_rrlo_*`, `gt`, `h4_r`). Не связаны с ними только три случайные контрольные, и все три либо `rrhi`, либо `a1`. Значит независимая область пространства — полоса RR ≥ 2 и фильтр согласия рук. У обеих нет зерна:

- **`rrhi`** измерена худшей полосой. S11: книга `sit` (RR ≥ 2) в минусе на всех шести горизонтах, книга `lo` в плюсе на пяти (`research/s11_horizon/out/S11-horizon-1m.md`). Подача: в последнюю неделю окна RR ≥ 2 оставляет 0–5 решений в сутки у деревьев и 3–63 у сети при 99–922 и 199–2446 гейтованных краем (`research/s8_loop/out/SHEET-supply-1m.md`). Это гейт живой `sit`, у которой медиана дня −14.48 $ на 20 сутках (`research/factory/out/STABILITY-live.md`).
- **`a1`** на 4 ч не измерен положительным: Δ среднего −3.1 б.п. (p 0.496, половины +81 / −106) у деревьев и +42.8 (p 0.145, половины +123 / −49) у сети (`research/probe_agree/out/AGREE-1m.md`). Знак не держится по половинам ни у одной руки. Плюс согласия живёт только на 24 ч (+441 / +358, p 0.000), а 24-часовой цели в листе нет: механика `12cc2578` заблокирована 08.09, просьба владельцу `4c923cf1` (роль fix) стоит без отметки `done` (`research/factory/out/asks.jsonl`).

Остальные оси — `w3`, `f44`, `sv`, `gs` — суть подмножества тех же решений: сосед по порогу, потолок закроет по независимости (правило из раздела 7 брифа).

**Про соблазн.** Единственный положительный форвард пула — случайная контрольная `h4_z_f30_w5_gs_rrhi_se_b0_a1`, +263.7 $ за 10 суток. Предлагать её соседа значит выбирать по увиденному форварду — ошибка R5, от которой построена вся конструкция.

**Вывод по строке:** любая строка сегодня либо повторяет занятую точку, либо стоит на неизмеренном (`a1` на 4 ч) или измеренно худшем (`rrhi`). Ни то ни другое не заявка.

## 2. Своя механика: каждый открытый угол брифа проверен

**Состояние очереди.** Пять механик построены и не прогнаны: `fcbd3542`, `994fc54f`, `63fa406a`, `49b535f8`, `a82dcf58`; шестая `13a67a67` построена 10.09 и откалибрована; `12cc2578` на владельце (`research/factory/out/mechanisms.jsonl`). Очередь строителя пуста — «нечего» (`research/factory/out/mech_task.json`). Новая механика строителю не стоит ничего, но встаёт седьмой к прогону — и оправдана только вопросом, которого те шесть не задают. Такого не нашлось:

- **Углы 4 и 5 — плечо с запасом σ при агрессивном гейте; размер по расстоянию до ликвидации.** Оба сводятся к разрезу и перевесу УЖЕ ЖИВОГО журнала `safe_h`, а не к новой книге. Правило безопасной короткой книги (запас 6σ) уже даёт полосы плеча от <3× до ≥ 20× в одной книге: полоса ≥ 20× — 106 сделок, +1756.56 $, +6.1 % на маржу; полоса <3× — 474 сделки, −32.50 $ (`research/dca_paper/out/DCA-short-why.md`). «Сестра 6σ + гейт ≥ 4×» есть та же книга без нижних полос, то есть лучшая полоса, выбранная после просмотра таблицы, — а её форвард журнал `safe_h` пишет бесплатно каждый день: `research/dca_paper/short_why.py` печатает полосы, нужна только объявленная дата отсчёта. «Размер по расстоянию до ликвидации» у книги без доливов есть маржа, обратная плечу (равный нотионал): исход позиции задан путём цены и плечом, деньги линейны по марже — это перевес того же журнала весами 1/L, минуты счёта тем же приёмом, каким `short_why` считал долю билета. Оговорка перевеса — касса и состав (с меньшей маржой книга взяла бы больше решений) — печатается рядом. Ни то ни другое не испытание: это запрос дежурного.
- **Угол 8 — сестра DCA с RR ≤ 1.5.** Вырождается: RR ≤ 1.5 оставляет 909 из 912 строк, гейтованных краем (04.09, деревья; медиана RR листа по суткам 0.54–1.20, чаще всего около 0.7). Это «отношение любое», и оно уже правило коротких книг `h24` (`research/dca_paper/rules.py`: «RR ≥ 2 душит подачу»). Длинная книга на таком гейте при 400 местах и 900–2300 решениях в сутки стоит полной всегда: состав решает очередь за кассой (`research/dca_paper/out/DCA-paper.md`: «отказ по кассе меняет не только объём, но и состав»), а деньги — рынок часа × плечо, то есть корзина без выходов, закрытая зондом. Честная сестра — лестница на топ-N листа по краю, а это четвёртое значение оси геометрии: смена пространства есть событие владельца (`space.py`, §10), не заявка предлагающего.
- **Угол 10 — открытый интерес поимённо.** В брифе нет ни одного числа ряда, а диагноз хвоста короткой стороны — «теряют редко и много, лечится размером, а не отбором» (`DCA-short-why.md`). Фильтр отбора по интересу шёл бы против нашего же замера; зерна нет.
- **Угол 15 — листинги против возраста:** взят вчера (`13a67a67`, `research/mech_13a67a67/run_age_mode.py`). **Угол 2** — построен там же. **Углы 3 и 11** — только календарь (38–49 сделок в полосах; t 1.06). **Углы 6 и 7** — на владельце. **Углы 9 и 12** — дефекты роли fix.
- **Углы 13–14 — лента и стакан.** Две построенные механики на событии D1 без боевого прогона (`research/mech_fcbd3542/run_halves.py`, `research/mech_994fc54f/bid_survives.py`). Третья на той же ленте была бы их соседом до их чисел — тот же довод, которым вчера отложен VPIN.

## 3. Меню разведчика (`research/factory/out/scout.json`, 11.09)

1. **Обратный перп против линейного.** Сборщик пишет только линейные; нужна новая запись 21 инструмента и потолок по ней. До записи убить нечем, объявлять нечего. Заметка дежурному: запись дешёвая (публичный `category=inverse`), но это решение о новом сборе, а не заявка.
2. **Кимчи-премия.** Новая запись (Upbit + курс вона) и покрытие имён числом ДО всего остального; сегодня не измеримо.
3. **DSR / SPA как нулевая модель.** Не испытание, а мера судьи: заменяет «максимум случайной полосы под нулём» (+201.6 на 10 зёрнах) формулой. Полезна и данных не требует — но менять правило, по которому судят, предлагающему запрещено. Заметка судье и дежурному, не слот.
4. **Перекос агрессии у круглых цен.** Единственная из четырёх, которую можно убить нашей лентой: доля агрессивных покупок в полосе под и над круглым числом против сдвинутой сетки на том же имени и часе; затем снос после пассивного заполнения в полосе против вне полосы тем же `fill_at` (`research/d1_seconds/passive.py`), которым намерены −28.8 против +25.9 б.п. (`research/d1_seconds/out/D1-passive-1m.md`). Это законная разделяющая переменная для условно закрытого пассивного входа — знак переворачивается, отбор съедает 95 % (`research/probe_calm_exec/out/CALM-exec-1s.md`). Не подаётся сегодня по одной причине: третья механика на ленте рядом с двумя непрогнанными на том же событии. Условие возврата — их отчёты на диске.

Запасные идеи прошлых прогонов (VPIN, доля счетов, делистинг, знак-не-величина, цензура ликвидаций, пересмотр риск-лимита) — либо новая запись, либо площадка не отдаёт истории (снятые контракты и тиры — текущее состояние), либо сосед непрогнанной механики.

## 4. Что откроет заявку

1. **Поля 24 ч в листе** (просьба `4c923cf1`) → строка `h24_z_f30_w5_gt_rr0_se_b0_a1` и близнец `a0`, уже названные механикой `12cc2578`. Связь книг ядра 24 ч ↔ 4 ч −0.055 — единственный способ поднять N пула, а не число строк.
2. **Боевые отчёты `fcbd3542` и `994fc54f`** → следующая механика ленты: круглые цены как условие исполнения лимита, либо забор короткой стороны по глубине книги (сосед `63fa406a`).
3. **Пересчёт согласия рук на 4 ч** на нынешних 34 сутках (было 22 на 30.08): если Δ > 0 при p ≤ 0.05 и знак держится по половинам у обеих рук — у `a1`-строки появляется зерно, и она попадает в независимую область пула.
4. **Месяц записи** → пороги возраста 7 / 30 / 60 различимы.

## 5. Бесплатные замеры дежурному (не заявки)

- Разрез форварда `safe_h` по полосам плеча с объявленной датой отсчёта — `short_why.py` уже печатает полосы, ноль строк кода.
- Перевес коротких журналов весами 1/плечо (равный нотионал) с оговоркой о кассе и составе — линейно, минуты, приём «доля билета» из `DCA-short-why.md`.
- Пересчёт `probe_agree` на 34 сутках — та же программа, данных в полтора раза больше.

Файлы заявки: `research/factory/out/proposal.json` (форма `{"proposed": false, "why": …}` плюс списки `checked`, `reopen_when`, `free_checks_for_duty`, `cites`), этот текст — человеческая форма того же.
