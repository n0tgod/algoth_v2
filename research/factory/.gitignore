# Сухой прогон роли и замок запускалки — машинное состояние, не
# артефакт. Настоящий журнал прогонов (out/agents-runs.jsonl) в git
# идёт намеренно: прогон, чья строка осталась на сервере, неотличим
# от прогона, которого не было.
out/agents-runs-dry.jsonl
out/agents.lock
out/cycle.log
out/cycle-*.log
