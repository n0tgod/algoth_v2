out/*
!out/*.md
!out/*.json
!out/*.html
!out/.gitkeep
out/*-smoke.*
out/status.json
out/token.txt
# Встречный счёт: производная от рядов, а не отчёт. Мегабайты сделок,
# меняющиеся при каждом прогоне, и восстановимые из записи за минуты.
out/recount.json
