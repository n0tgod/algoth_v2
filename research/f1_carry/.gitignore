chunks/
vectors/
# Смоук на чужих ставках — артефакт с отдельным именем, в git не идёт.
# Чистить его каталогом целиком нельзя: `rm -rf out` уже один раз снёс
# из репозитория артефакт настоящего прогона с VPS (коммит 5f0166b).
out/f1_*_binance.json
out/F1-report-*-binance.md
